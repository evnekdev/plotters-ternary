[package]
name = "plotters-ternary"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = { version = "0.3.7", default-features = false, features = [
    "bitmap_backend",
    "bitmap_encoder",
    "svg_backend",
    "ttf",
] }

[dev-dependencies]
proptest = "1.6"
